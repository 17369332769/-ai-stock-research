"""OpenBB 沪深300（CSI300）成分 Provider。

spec §4.1 / §5.2：**以中证指数官方成分及调整公告为权威来源**。
spec §9.3：训练样本按每个交易日当时有效的 CSI300 历史成员生成，
**禁止用当前 300 只股票回填全部历史** —— 那是教科书级的幸存者偏差。

因此本 Provider 有两条**语义不同**的路径，绝不互相顶替：

1. ``as_of >= 今天``  → 中证指数官网当期成分文件（当前成分，用于选股）。
2. ``as_of <  今天``  → 官方历史成分快照归档（用于训练）。归档缺失 → **直接报错**，
   绝不用"当前 300 只"冒充历史成分（fail closed，spec §5.2 不做静默备用源）。

归档由本 Provider 在每次成功抓取当期成分时写入（write-through），因此系统上线越久，
可用的无偏历史越长；上线之前的历史需要人工从中证官网补齐 —— 这个边界写在
docs/data-sources.md，不在代码里假装已经有。
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0"
